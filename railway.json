{
  "$schema": "https://railway.app/schema.json",
  "build": {
    "builder": "dockerfile",
    "dockerfile": "Dockerfile.railway"
  },
  "deploy": {
    "startCommand": "cd backend && php -S 0.0.0.0:${PORT:-8000}",
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 5
  }
}
